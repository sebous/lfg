import styled from "styled-components";

export const UserIconSliderWrapper = styled.div`
  padding: 0.5rem 0.25rem;
  border-bottom: 1px solid ${props => props.theme.color.gray};
  height: 78px;
  overflow: hidden;
`;
